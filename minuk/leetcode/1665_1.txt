class Solution:
    def minimumEffort(self, tasks: List[List[int]]) -> int:
        tasks.sort(key = lambda x: (x[1] - x[0]), reverse=True)
        res = 0
        curr = 0
        for a, b in tasks:
            if (curr < b):
                res += b - curr
                curr = b
            curr -= a

        return res